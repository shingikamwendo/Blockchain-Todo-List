//smart contrac that will manage the todolist for the application

pragma solidity ^0.5.0;     //version for solidity

//declaring the smart contract
contract TodoList {
    uint public taskCount = 0; //state variable(represent the state of the smart contract on the blockchain)
}